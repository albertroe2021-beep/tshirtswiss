# Dev Container for tshirtswiss

This Codespaces/devcontainer setup provides a Python 3.12 environment suitable for the repository's scripts and local static-site preview workflow.

## Useful commands
- Start a preview server: python3 -m http.server 8080
- Run the SVG mapping script: python3 scripts/apply_svg_icon_mapping.py --dry-run --verbose
